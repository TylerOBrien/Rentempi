/**
 * Global Imports
*/

import Config from 'react-native-config';

/**
 * Exports
*/

export const BuildConfig = {
  code: Config.BUILD_CODE,
  version: Config.BUILD_VERSION,
  builtAt: Config.BUILD_BUILT_AT
};
