/**
 * Global Imports
*/

import {
  BUILD_CODE,
  BUILD_VERSION,
  BUILD_BUILT_AT
} from 'react-native-config';

/**
 * Exports
*/

export const BuildConfig = {
  code: BUILD_CODE,
  version: BUILD_VERSION,
  builtAt: BUILD_BUILT_AT
};
